---
menuCode: MST_WAREHOUSE
audience: user
title: 仓库/位置管理
summary: 注册和管理仓库主数据·仓库内详细位置·仓库间移动规则的画面
tags: [基础信息, 仓库, 位置, 库存]
keywords: [仓库, 仓库代码, 仓库名称, 仓库类型, 位置, 装载位置, 详细位置, 区域, 区划, 货架, 行, 列, 层, 移动规则, 搬运规则, 出发仓库, 到达仓库, 允许, 禁止, 默认仓库, 原材料仓库, 半成品仓库, 成品仓库, 工序在制品, 不良仓库, 报废仓库, 外协处]
related: [MST_PART]
---

# 仓库/位置管理

## 画面目的
注册和管理库存存放·移动的**仓库**、仓库内的**详细装载位置（位置）**以及仓库间的**移动允许规则**。此处登记的仓库/位置是入库·发放·库存·生产实绩·库存移动的基础。

## 画面构成
上方标签分为3个区域。右上角按钮（刷新/登记）根据所选标签切换。

- **仓库管理标签**：仓库主数据列表（左侧网格）+ 搜索·仓库类型过滤。行✏️（修改）/🗑️（删除），右上角**新建登记**。点击时打开登记/修改模态框。
- **位置标签**：仓库内详细位置列表 + 仓库过滤。位置的登记/修改/删除。
- **仓库移动规则标签**：出发仓库→到达仓库移动允许/禁止规则列表 + 规则添加/修改/删除。

三个区域的关系：
```
仓库(WAREHOUSES)
  ├─ 位置(WAREHOUSE_LOCATIONS)  : 一个仓库内有多个详细位置
  └─ 移动规则(WAREHOUSE_TRANSFER_RULES) : 仓库↔仓库移动允许/禁止
```

---

## ① 仓库管理标签 — 列/表单字段

| 列/字段 | 作用 / 含义 |
|------|------|
| **仓库代码(warehouseCode)** | 标识仓库的**唯一代码**。注册后不可更改（库存·实绩·移动规则通过此代码关联）。 |
| **仓库名称(warehouseName)** | 现场识别的仓库名称。 |
| **仓库类型(warehouseType)** | 仓库的用途分类。入库·库存·生产处理方式据此值变化。值：原材料仓库(RAW)·半成品仓库(WIP)·成品仓库(FG)·工序在制品(FLOOR)·不良仓库(DEFECT)·报废仓库(SCRAP)·外协处(SUBCON)。 |
| **线别(lineCode)** | **工序在制品(FLOOR)**仓库所属的生产线。仅在类型为FLOOR时输入。 |
| **工序(processCode)** | **工序在制品(FLOOR)**仓库所属的工序。仅在类型为FLOOR时输入。 |
| **默认(isDefault)** | 是否为同一类型中默认使用的仓库。自动入库/放置时按类型优先选择默认仓库。 |
| **使用(useYn)** | 仅`Y`时为查询·选择·使用对象。停用为`N`。 |

> 仅仓库类型为**工序在制品(FLOOR)**时显示线别·工序输入框。其他类型不使用线别/工序。

## ② 位置标签 — 列/表单字段

一个仓库可设置多个位置（bin/rack/shelf等详细位置）。

| 列/字段 | 作用 / 含义 |
|------|------|
| **仓库(warehouseCode)** | 该位置所属的仓库。注册后不可更改。 |
| **位置代码(locationCode)** | 标识仓库内详细位置的代码。注册后不可更改（仓库+位置代码组合唯一）。 |
| **位置名称(locationName)** | 现场识别的位置名称。 |
| **区域(zone)** | 仓库内区域划分值。 |
| **行(rowNo)** | 货架/搁板的行编号。 |
| **列(colNo)** | 货架/搁板的列编号。 |
| **层(levelNo)** | 货架/搁板的层编号。 |
| **使用(useYn)** | 仅`Y`时为使用对象（列表中以●绿色点显示）。 |
| **备注(remark)** | 位置管理参考事项。 |

## ③ 仓库移动规则标签 — 列/表单字段

定义从出发仓库到到达仓库的库存移动是允许还是禁止。一对（出发→到达）为1条规则。

| 列/字段 | 作用 / 含义 |
|------|------|
| **出发仓库(fromWarehouse) / 出发仓库代码·出发仓库名称** | 移动的出发仓库。在表单中从仓库列表选择。 |
| **到达仓库(toWarehouse) / 到达仓库代码·到达仓库名称** | 移动的到达仓库。 |
| **允许与否(allowYn)** | 该路径的移动允许与否。`允许(Y)`=出发→到达可移动，`禁止(N)`=阻止。 |
| **备注(remark)** | 移动规则管理参考事项。 |

---

## 使用顺序
1. 在**仓库管理标签**中通过右上角**新建登记**创建仓库（仓库代码·仓库名称·仓库类型必填，FLOOR时还需线别·工序）。
2. 如需在**位置标签**中为该仓库登记详细位置（区域/行/列/层）。
3. 如需控制仓库间移动，在**仓库移动规则标签**中登记出发→到达配对并指定允许/禁止。
4. 按类型指定用于自动放置的仓库时勾选**默认**复选框。

## 输入规则 / 校验
- 仓库代码·位置代码注册后不可更改（修改时代码框禁用）。
- 同一仓库代码、同一（仓库+位置）组合、同一（出发→到达）配对不可重复登记。
- **有库存残留的仓库不可删除。**

## 常见问题
- **问：** 线别/工序输入框不显示。
  **答：** 需将仓库类型选为**工序在制品(FLOOR)**后才显示线别·工序输入框。
- **问：** 删除仓库时失败。
  **答：** 该仓库有残留库存时删除被拒绝。请清空库存后重试，或将使用与否改为`N`停用。
- **问：** 未登记移动规则的仓库配对能否移动？
  **答：** 根据运营政策而定。明确设定了**禁止(N)**规则的路径将被阻止（参照运营指南）。

## 相关画面
- [物料主数据](/master/part) — 按物料设置默认装载位置
